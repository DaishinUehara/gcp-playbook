---
# tasks file for role1
- name: debug message role 1
  debug: msg="role1"

- block:
  - name: debug message development_env
    debug: msg="グループ階層1 {{ development.grp1 }}"

  - name: debug message local_dev
    debug: msg="グループ階層2 {{ development.localdev.grp2 }}"

  - name: debug message host
    debug: msg="ホスト変数 {{ development.local_dev.localhost.hst }}"

  tags: dev

- block:
  - name: debug message production_env
    debug: msg="グループ階層1 {{ production.grp1 }}"

  - name: debug message local_dev
    debug: msg="グループ階層2 {{ production.localdev.grp2 }}"

  - name: debug message host
    debug: msg="ホスト変数 {{ production.local_prd.localhost.hst }}"

  tags: prd
